# Optional: override the RPC endpoint (defaults to the public devnet endpoint)
# Use a dedicated RPC (e.g. Helius, QuickNode) for better rate limits in production.
VITE_RPC_URL=https://api.devnet.solana.com
